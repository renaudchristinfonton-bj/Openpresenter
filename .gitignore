# Données utilisateur synchronisées par le serveur relais (créées à l'exécution)
servergestionobs/data/

# Tests : dépendances npm locales (jouer « npm i » dans servergestionobs/tests)
servergestionobs/tests/node_modules/
